# Modo B2B para desarrollo local (vite --mode b2b)
# No es estrictamente necesario — detectAppMode() hace fallback a 'b2b'.
# Existe por consistencia con .env.marketplace y .env.alquiler.
VITE_APP_MODE=b2b
